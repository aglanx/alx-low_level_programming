#include <cstdio.h>

/**
 * main - prints the alphabet in lowercase
 *
 * Return: Alwyays 0.
 */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
	{
		putchar(letter);
	}

	return (0);

}
